{
  "name": "chicagorail",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --stream dev",
    "build": "pnpm --filter @chicagorail/shared build && pnpm --filter @chicagorail/backend build",
    "heroku-postbuild": "pnpm --filter @chicagorail/shared build && pnpm --filter @chicagorail/backend build && ls -la packages/backend/dist/src/",
    "heroku-cleanup": "echo 'Skipping cleanup'",
    "backend:dev": "pnpm --filter @chicagorail/backend dev",
    "frontend:dev": "pnpm --filter @chicagorail/frontend dev",
    "shared:build": "pnpm --filter @chicagorail/shared build",
    "start": "node packages/backend/dist/src/index.js"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
